# Verified Phase 2 results

These compact metrics and generated reports correspond to the resolved configuration and manifest in this directory. Individual picks are stored as deterministic `picks.csv.gz` (the exact exported CSV, compressed). Forecast matrices and the research/frozen databases remain in the ignored `data/experiments/phase2-validation` directory.

Published with `uv run python experiments/publish_phase2.py` after `uv run python experiments/verify_phase2.py` and a complete benchmark resume check. See [schema and reproduction instructions](../../README.md).

Matching source implementation: `79c79f2166cb3b5ac073a8f00670b00562b8fda4`. Historical input timing remains approximate; both eras are retrospective. No production parameter was tuned or calibrated in this phase.
