.venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
data/*.db
data/*.db-journal
.pytest_cache/
.ruff_cache/
.uv-cache/

# uv.lock is intentionally committed — it pins the exact environment.
data/experiments/

# Delivered pool reports name every entrant; keep them out of the repository.
data/reports/

# Backups are copies of the database, and hold everything it does.
data/backups/
