# Commits `git blame` should look through: mechanical changes that moved every line they
# touched without changing what it does. GitHub reads this file automatically; locally, run
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
# The hashes only hold if these commits are merged as they are, not squashed or rebased.

# Format src and tests with ruff
8bca7cbd0d61c0a66d2dc829e441a86144773d4f
