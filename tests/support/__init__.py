"""Builders shared by the tests: databases, frames, reports and captured decisions.

Test modules import from here and never from one another, so moving or deleting one
suite cannot break another.
"""
